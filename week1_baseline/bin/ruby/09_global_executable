#!/usr/bin/env bash

# Step 09 ships no examples/ — bin/boukensha is its entry point, so this launcher runs that
# rather than an example script. Config resolves by walking up from the step directory to the
# repo root's .boukensha.
cd "$(dirname "$0")/../../ruby/09_global_executable"
bundle exec ruby bin/boukensha
