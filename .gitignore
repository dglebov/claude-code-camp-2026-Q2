# Windows Zone.Identifier files (WSL download metadata)
*:Zone.Identifier

# Environment / secrets
.env
.env.local

# Agent session logs (step 06+). Each line carries the full conversation, including the contents
# of any file the agent's tools read, so these are both noisy and potentially sensitive.
.boukensha/sessions/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
